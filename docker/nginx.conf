server {
    listen 80;
    client_max_body_size 2m;
    location /static/ { alias /app/static/; expires 7d; }
    location / { proxy_pass http://app:8000; proxy_set_header Host $host; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
}
