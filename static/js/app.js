function showToast(message,type='success'){const toast=document.querySelector('#toast');if(!toast)return;toast.textContent=message;toast.dataset.type=type;toast.hidden=false;setTimeout(()=>toast.hidden=true,4000)}
window.renderChart=function(id,type,labels,data,title){const canvas=document.getElementById(id);if(!canvas||!window.Chart)return;new Chart(canvas,{type,data:{labels,datasets:[{label:title,data,backgroundColor:['#00b4d8','#8b5cf6','#02c39a','#f2cc60']}]},options:{responsive:true,plugins:{legend:{display:false}}}})};
document.addEventListener('submit',event=>{const button=event.target.querySelector('button[type=submit]');if(button){button.disabled=true;button.textContent='Working...'}});
document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.nav-toggle');const links=document.querySelector('.nav-links');if(toggle&&links)toggle.addEventListener('click',()=>{const open=links.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open))});document.querySelectorAll('.query-chips button').forEach(chip=>chip.addEventListener('click',()=>{const input=document.querySelector('textarea[name=question]');if(input){input.value=chip.textContent;input.focus()}}));document.querySelectorAll('.quality-ring').forEach(ring=>ring.style.setProperty('--score',ring.dataset.score));});
document.addEventListener('click',event=>{const button=event.target.closest('.triage-filters button');if(!button)return;document.querySelectorAll('tr[data-priority]').forEach(row=>row.hidden=button.dataset.filter!=='All'&&row.dataset.priority!==button.dataset.filter);});
